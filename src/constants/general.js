export const config = require('../../config');

export const messages = {
    error: {
        badLogin: 'Not a valid login or password, try again',
        undef: 'Something went wrong'
    }
}

export const storageKeys = {
    auth: 'AUTH' ,
    rep: 'REPOSITORIES'
}